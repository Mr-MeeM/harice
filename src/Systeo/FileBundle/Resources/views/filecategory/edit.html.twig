{% form_theme edit_form 'bootstrap_3_layout.html.twig' %}
{% extends 'layout.html.twig' %}

{% block body %}
    <fieldset>
            <legend>{{ 'Modifier catégorie de tâche'}}</legend>
    </fieldset>
    <div class="row">
        <div class="col-lg-12">      
            <div class="btn_list pull-right">
                <a href="{{ path('filecategory_index') }}"  class="btn btn-info">Liste <span class="glyphicon glyphicon-th-list"></span></a>
            </div>
        </div>
    </div>

    {{ form_start(edit_form) }}
    <div class="row">
        <div class="col-lg-4">
            {{ form_row(edit_form.name) }}
        </div>
        <div class="col-lg-4">
            <br />
            {{ form_row(edit_form.active) }}
        </div>
    </div>
     <input type="submit" value="Enregistrer" class="btn btn-success"/>
    {{ form_end(edit_form) }}

{% endblock %}
